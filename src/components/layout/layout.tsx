export const Layout = ({children}) => {
    return (
        <main>
            <header>
                heder
            </header>
            {children}
            <footer>
                futer
            </footer>
        </main>
    )
}